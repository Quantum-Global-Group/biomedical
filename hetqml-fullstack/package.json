{
  "name": "hetqml-fullstack",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "sh -c 'cd apps/web && PORT=\"${HETQML_WEB_PORT:-3000}\" pnpm exec next dev'",
    "dev:api": "sh -c 'cd apps/api && uv run uvicorn hetqml_api.main:app --reload --host 0.0.0.0 --port \"${HETQML_API_PORT:-8000}\"'",
    "test:web": "pnpm --filter hetqml-web test",
    "test:api": "cd apps/api && uv run pytest",
    "test": "pnpm test:web && pnpm test:api",
    "build:web": "pnpm --filter hetqml-web build"
  },
  "packageManager": "pnpm@9.12.0"
}
